{
  "name": "practices-ui",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "typings": "bundle.d.ts",
  "scripts": {
    "build": "webpack",
    "watch": "tsc-watch --build --incremental",
    "tsc": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "typescript": "beta"
  },
  "dependencies": {
    "react": "^17.0.1",
    "practices-common": "workspace:*"
  }
}
