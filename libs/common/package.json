{
    "name": "@practices/common",
    "version": "1.0.0",
    "private": true,
    "exports": {
        ".": {
            "import": {
                "types": "./src/index.mts",
                "default": "./dist/index.mjs"
            },
            "require": null
        }
    },
    "scripts": {
        "build": "tsc --build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "typescript": "beta"
    }
}
